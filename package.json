{
  "name": "who-is-right",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://mazharz.github.io/who-is-right/",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "gh-pages": "^6.1.1",
    "preact": "^10.19.6"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.8.2",
    "@testing-library/jest-dom": "^6.4.5",
    "@testing-library/preact": "^3.2.3",
    "@vitest/coverage-v8": "^1.6.0",
    "jsdom": "^24.0.0",
    "typescript": "^5.2.2",
    "vite": "^5.2.0",
    "vitest": "^1.6.0"
  }
}
